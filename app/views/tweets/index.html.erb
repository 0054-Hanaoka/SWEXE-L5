<% @users.each do |t| %>
  <p>
    <%= t.uid %> | <%= t.pass %><br>
    <%= link_to '削除', tweet_path(t),  data: {turbo_method: :delete, turbo_confirm: "本当に削除しますか？"} %>
  </p>
<% end %>

<%= link_to '新規登録', new_tweet_path %>